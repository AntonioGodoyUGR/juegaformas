# JuegaFormas

## Agent skills

### Issue tracker

Markdown local: las issues y specs viven en `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Los cinco roles canónicos, sin renombrar. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` y `docs/adr/` en la raíz. See `docs/agents/domain.md`.
